apiVersion: v1
kind: Service
metadata:
  name: sm-procurement
  namespace: sm
  labels:
    app: sm-procurement
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-procurement
  ports:
  - name: http
    port: 8530
    targetPort: http
    protocol: TCP
